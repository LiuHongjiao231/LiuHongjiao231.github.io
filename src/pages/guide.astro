---
import Layout from '../layouts/Layout.astro';
---
<Layout title="更新指南">

  <div class="max-w-4xl mx-auto px-4 pt-8">
    <a href="/" class="inline-flex items-center gap-2 text-gray-500 hover:text-indigo-600 transition text-sm">
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/></svg>
      返回首页
    </a>
  </div>

  <section class="py-16 md:py-24 bg-gradient-to-br from-indigo-50 via-white to-rose-50">
    <div class="max-w-4xl mx-auto px-4">
      <h1 class="text-3xl md:text-4xl font-bold text-center mb-4">Vibe Coding 建站 & 内容更新指南</h1>
      <p class="text-center text-gray-500 mb-16">本文档记录了本网站的建站方式与内容更新流程</p>

      <!-- 建站方式 -->
      <div class="bg-white rounded-2xl border border-gray-100 p-8 mb-8">
        <h2 class="text-xl font-bold mb-4">一、建站方式</h2>
        <p class="text-gray-600 text-sm leading-relaxed mb-4">
          本作品集网站采用 <span class="font-medium text-indigo-600">Vibe Coding</span>（氛围编程）的方式完成，即通过 AI 对话式编程工具，以自然语言描述需求，由 AI 生成代码并迭代优化，全程无需手写代码。
        </p>
        <div class="grid md:grid-cols-2 gap-4 text-sm">
          <div class="p-4 bg-indigo-50 rounded-xl">
            <h3 class="font-bold text-indigo-900 mb-2">技术栈</h3>
            <ul class="text-gray-600 space-y-1">
              <li>· Astro — 静态网站框架</li>
              <li>· Tailwind CSS — 样式系统</li>
              <li>· GitHub Pages — 免费托管部署</li>
              <li>· GitHub Actions — 自动化构建</li>
            </ul>
          </div>
          <div class="p-4 bg-rose-50 rounded-xl">
            <h3 class="font-bold text-rose-900 mb-2">AI 工具</h3>
            <ul class="text-gray-600 space-y-1">
              <li>· OpenCode — AI 编程助手</li>
              <li>· 通过对话生成页面、样式、内容</li>
              <li>· AI 负责代码编写与调试</li>
              <li>· 我负责内容策划与决策</li>
            </ul>
          </div>
        </div>
      </div>

      <!-- 项目结构 -->
      <div class="bg-white rounded-2xl border border-gray-100 p-8 mb-8">
        <h2 class="text-xl font-bold mb-4">二、项目结构</h2>
        <div class="bg-gray-50 rounded-xl p-6 font-mono text-sm text-gray-700 space-y-1">
          <p>my-website/</p>
          <p class="pl-4">├── src/</p>
          <p class="pl-8">├── pages/          <span class="text-gray-400">← 各页面文件</span></p>
          <p class="pl-12">├── index.astro      <span class="text-gray-400">← 首页</span></p>
          <p class="pl-12">├── academic.astro   <span class="text-gray-400">← 学术研究</span></p>
          <p class="pl-12">├── marketing.astro  <span class="text-gray-400">← 营销作品</span></p>
          <p class="pl-12">├── skills.astro     <span class="text-gray-400">← 技能特长</span></p>
          <p class="pl-12">└── guide.astro      <span class="text-gray-400">← 本文档</span></p>
          <p class="pl-8">├── layouts/</p>
          <p class="pl-12">└── Layout.astro     <span class="text-gray-400">← 公共布局</span></p>
          <p class="pl-8">└── styles/</p>
          <p class="pl-12">└── global.css       <span class="text-gray-400">← 全局样式</span></p>
          <p class="pl-4">├── public/</p>
          <p class="pl-8">└── images/           <span class="text-gray-400">← 图片资源</span></p>
          <p class="pl-4">├── dist/              <span class="text-gray-400">← 构建产物（部署用）</span></p>
          <p class="pl-4">├── astro.config.mjs   <span class="text-gray-400">← Astro 配置</span></p>
          <p class="pl-4">└── package.json       <span class="text-gray-400">← 项目依赖</span></p>
        </div>
      </div>

      <!-- 更新内容 -->
      <div class="bg-white rounded-2xl border border-gray-100 p-8 mb-8">
        <h2 class="text-xl font-bold mb-4">三、如何更新内容</h2>

        <div class="space-y-6">
          <!-- 修改文字 -->
          <div class="p-5 bg-gray-50 rounded-xl">
            <h3 class="font-bold mb-2">1. 修改文字内容</h3>
            <p class="text-sm text-gray-600 mb-3">打开对应的 <code class="bg-gray-200 px-1.5 py-0.5 rounded text-xs">.astro</code> 文件，直接编辑文字即可。</p>
            <div class="bg-white rounded-lg p-4 text-sm font-mono text-gray-600">
              <p class="text-gray-400"># 例如修改首页个人简介，打开 src/pages/index.astro</p>
              <p>找到对应文字，直接修改 → 保存</p>
            </div>
          </div>

          <!-- 替换图片 -->
          <div class="p-5 bg-gray-50 rounded-xl">
            <h3 class="font-bold mb-2">2. 替换图片</h3>
            <p class="text-sm text-gray-600 mb-3">将新图片放入 <code class="bg-gray-200 px-1.5 py-0.5 rounded text-xs">public/images/</code> 文件夹，然后在代码中修改图片路径。</p>
            <div class="bg-white rounded-lg p-4 text-sm font-mono text-gray-600">
              <p class="text-gray-400"># 例如替换个人照片</p>
              <p>&lt;img src="/images/新照片.jpg" alt="刘红娇" /&gt;</p>
            </div>
          </div>

          <!-- 添加新页面 -->
          <div class="p-5 bg-gray-50 rounded-xl">
            <h3 class="font-bold mb-2">3. 添加新页面</h3>
            <p class="text-sm text-gray-600 mb-3">在 <code class="bg-gray-200 px-1.5 py-0.5 rounded text-xs">src/pages/</code> 下新建 <code class="bg-gray-200 px-1.5 py-0.5 rounded text-xs">xxx.astro</code> 文件，参考已有页面编写即可。</p>
          </div>

          <!-- 部署上线 -->
          <div class="p-5 bg-gray-50 rounded-xl">
            <h3 class="font-bold mb-2">4. 部署上线</h3>
            <p class="text-sm text-gray-600 mb-3">修改完成后，需要执行构建并将产物推送到 GitHub：</p>
            <div class="bg-white rounded-lg p-4 text-sm font-mono text-gray-600 space-y-1">
              <p class="text-gray-400"># 第1步：构建网站</p>
              <p>npm run build</p>
              <p class="text-gray-400 mt-2"># 第2步：将 dist/ 内容复制到仓库根目录</p>
              <p class="text-gray-400"># 第3步：推送到 GitHub</p>
              <p>git add .</p>
              <p>git commit -m "更新内容"</p>
              <p>git push</p>
            </div>
            <p class="text-sm text-gray-600 mt-3">推送后 1-2 分钟，网站会自动更新。</p>
          </div>
        </div>
      </div>

      <!-- 本地预览 -->
      <div class="bg-white rounded-2xl border border-gray-100 p-8 mb-8">
        <h2 class="text-xl font-bold mb-4">四、本地预览（可选）</h2>
        <p class="text-sm text-gray-600 mb-3">如需在本地查看修改效果，可以启动开发服务器：</p>
        <div class="bg-gray-50 rounded-lg p-4 text-sm font-mono text-gray-600 space-y-1">
          <p class="text-gray-400"># 安装依赖（首次）</p>
          <p>npm install</p>
          <p class="text-gray-400 mt-2"># 启动开发服务器</p>
          <p>npm run dev</p>
          <p class="text-gray-400 mt-2"># 浏览器打开 http://localhost:4321 即可预览</p>
        </div>
      </div>

      <!-- 总结 -->
      <div class="bg-gradient-to-r from-indigo-500 to-rose-500 rounded-2xl p-8 text-white">
        <h2 class="text-xl font-bold mb-3">总结</h2>
        <p class="text-sm leading-relaxed opacity-90">
          本作品集网站采用 Vibe Coding 方式，借助 AI 编程工具完成开发。整个过程中，我负责内容策划、页面结构设计和最终决策，AI 负责代码生成与调试。网站使用 Astro 框架构建，部署在 GitHub Pages 上，支持免费托管和自动更新。
        </p>
      </div>

    </div>
  </section>

</Layout>
